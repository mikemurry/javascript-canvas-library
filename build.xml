<?xml version="1.0" encoding="UTF-8"?>

<project name="VentBuild" default="dist">

    <property name="includes" value="vent.js, vent.utilities.js, vent.renderer.js, vent.canvas.js, vent.performance.js" />

    <target name="prepare">
        <delete dir="temp" />
        <mkdir dir="temp" />
        <copy todir="temp">
            <fileset dir="lib" />
        </copy>
    </target>

    <target name="minify" depends="prepare">
        <foreach param="filename" absparam="absfilename" target="minify.file">
            <fileset dir="temp" includes="*.js" />
        </foreach>
    </target>

    <target name="minify.file">
        <echo msg="Compressing File: ${absfilename}" />
        <exec command="java -jar C:\phing\tools\yuicompressor-2.4.7\build\yuicompressor-2.4.7.jar ${absfilename} -o ${absfilename}" />
    </target>

    <target name="build" depends="minify">
        <delete file="vent.js" />
        <append destFile="vent.js">
            <filelist dir="lib" files="${includes}" />
        </append>
        <delete file="vent.min.js" />
        <append destFile="vent.min.js">
            <filelist dir="temp" files="${includes}" />
        </append>
    </target>

    <target name="document" depends="build">
        <exec command="java -jar C:\phing\tools\jsdoc-toolkit\jsrun.jar C:\phing\tools\jsdoc-toolkit\app\run.js vent.js -p -t=C:\phing\tools\jsdoc-toolkit\templates\jsdoc -d=doc" />
    </target>

    <target name="dist" depends="document">
        <delete dir="temp" />
    </target>

</project>